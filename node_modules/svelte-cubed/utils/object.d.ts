/**
 * @param {import('three').Object3D} object
 * @param {import('../types/common').Position} position
 * @param {import('../types/common').Rotation} rotation
 * @param {import('../types/common').Scale} scale
 */
export function transform(object: import('three').Object3D, position: import('../types/common').Position, rotation: import('../types/common').Rotation, scale: import('../types/common').Scale): void;
